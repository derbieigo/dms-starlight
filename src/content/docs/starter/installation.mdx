---
title: Installations
description: A guide to running this docs
---

import { Code, Tabs, TabItem, Aside } from '@astrojs/starlight/components';

<Aside>This is a sample documentation only.</Aside>

To clone a project, follow these steps:
<Tabs>
    <TabItem label="HTTPS">
        ```sh
        git clone https://github.com/derbieigo/dms-starlight.git
        ```
    </TabItem>
    <TabItem label="SSH">
        ```
        git clone git@github.com:derbieigo/dms-starlight.git
        ```
    </TabItem>
</Tabs>

export const exampleCode = `npm run dev`;
export const fileName = 'HTTPS';
export const highlights = ['file', 'CMS'];

After cloning, run this to your command line
```
npm run dev
```
Now you can access and run the project.

Access [starlight documentation](https://starlight.astro.build/).